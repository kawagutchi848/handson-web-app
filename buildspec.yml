version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name Handson-WebServer --s3-location s3://Handson-WebServer/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src